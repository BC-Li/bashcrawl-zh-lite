
# 你可以与房间里的物品进行互动（它们以*结尾）。
# 通过运行它们作为一个命令（一个shell脚本）。
#
# 例如，要收集宝藏。
#
# ./treasure
# 
# .（点）表示*不要移动*，而脚本的名字 
# 脚本使代码运行。 这被称为 
# 相对路径, 而.表示当前房间
# (目录).
# 
# 你可能在前一个房间留下了宝藏。
# 用两个点回到前一个目录。
# 意思是*退一步*。
#
# 举个例子。
#
# cd ..